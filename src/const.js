// Request fallback types
const FALLBACK_PROXY = 'proxy';
const FALLBACK_MOCK = 'mock';

// Mock types
const MOCK_TYPE_NORMAL = 'normal';
const MOCK_TYPE_MOCKJS = 'mockjs';

module.exports = {
    FALLBACK_PROXY,
    FALLBACK_MOCK,

    MOCK_TYPE_NORMAL,
    MOCK_TYPE_MOCKJS,
};
