// Request fallback types
const FALLBACK_PROXY = 'proxy';
const FALLBACK_MOCK = 'mock';

// Mock types
const MOCK_TYPE_NORMAL = 'normal';
const MOCK_TYPE_MOCKJS = 'mockjs';

// Gulp Mocker Meta Keys
const GP_META_KEY_FILE_LEVEL_HEADERS = 'GP_META_KEY_FILE_LEVEL_HEADERS';

module.exports = {
    FALLBACK_PROXY,
    FALLBACK_MOCK,

    MOCK_TYPE_NORMAL,
    MOCK_TYPE_MOCKJS,
};
